interface:
  display_name: Visual Review
  short_description: Inspect immutable matched visual evidence without editing.
  default_prompt: "Use $visual-review to inspect this hash-bound capture packet and return a verdict without editing files."
