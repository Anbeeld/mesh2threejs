---
name: finalize
description: Fail-closed certification and user handoff for a fully evidenced procedural reconstruction. Use only after fresh deterministic, style, articulation, critic, and turntable evidence exists.
---

# Finalize

Run `mesh2threejs finalize <state.json>`. Certification requires current-hash evidence for registration, deterministic gate, style, complexity, articulation, separate-process critic, and turntable, with no blocking unresolved item. `exact-real` tanks additionally require admitted authoritative dimensions; never relabel oracle-relative results.

Deliver candidate source, optional export if produced, board, turntable, geometry/style reports, intentional simplifications and omissions, oracle attribution/redistribution requirements, and the final hash. Any requested adjustment reopens the affected phases.
