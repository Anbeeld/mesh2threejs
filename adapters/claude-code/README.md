# Claude Code adapter

This is a host-neutral launch configuration, not a certification claim. The executable was absent during validation. Once installed, verify local instruction/skill discovery, nested profile loading, command/browser access, and critic isolation; then update the manifest flags with captured evidence.
