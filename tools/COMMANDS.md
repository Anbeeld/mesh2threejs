# Tool surface

The compiled `mesh2threejs` CLI is the canonical tool surface. It provides workspace/state, oracle probe/onboarding, candidate audit, gates/workorders, separate critic, and finalization. Rendering boards/turntables are exposed by the library and exercised in `src/validation/e2e.ts`.
