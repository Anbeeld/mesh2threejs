# Tank kit contract

Use `createLoftGeometry` from `src/kit.ts` for reproducible raked hull/plate masses and native Three.js primitives for wheels, turret, barrel, tracks, and fittings. Each mesh/group receives a stable `semanticId`; moving parts live under explicit pivots. The kit is a construction vocabulary, never cached oracle topology.
